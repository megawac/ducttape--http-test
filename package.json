{
  "name": "ducttape-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-ducttape": "^1.0.3",
    "shrinkpack": "^0.13.1"
  },
  "scripts" : {
    "shrinkpack": "rm -f npm-shrinkwrap.json && npm shrinkwrap --dev && shrinkpack",
    "ducttape": "rm -f npm-shrinkwrap.json && npm shrinkwrap --dev && npm-ducttape"
  }
}
